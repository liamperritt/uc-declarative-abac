definitions:
  functions:
    abac|fn_mask_pii:
      name: fn_mask_pii
      owner: liam.perritt@databricks.com
      comment: Function that masks a given PII value
      parameters:
        - name: pii
          type: string
      return: "CONCAT('***', SUBSTRING(pii, -4))"
