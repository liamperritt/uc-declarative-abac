definitions:
  functions:
    abac|format_phone:
      name: format_phone
      owner: sp_uc_governor_test
      parameters:
        - name: code
          type: string
        - name: phone
          type: string
      return: "concat('+', code, phone)"
