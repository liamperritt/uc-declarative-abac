definitions:
  functions:
    abac|format_phone:
      $vars:
        env: ~
        redaction_character: '+'
      name: format_phone
      owner: sp_uc_governor_{{ env }}
      rfa_destinations:
        - liam.perritt@databricks.com
      parameters:
        - name: code
          type: string
        - name: phone
          type: string
      return: "concat('{{ redaction_character }}', code, phone)"
