definitions:
  schemas:
    liam_perritt|lff_sqlserver_bronze:
      $vars:
        tag_prefix: uc_gov
      name: lff_sqlserver_bronze
      tags:
        '{{ tag_prefix }}_team': data_engineering
        '{{ tag_prefix }}_zone': bronze
      policies:
        - $ref: $defs/policies/pii|mask_pii
          $vars:
            pipeline: lff
      tables:
        - $ref: $defs/tables/liam_perritt|lff_sqlserver_bronze|dummy_cdc_sink
          columns:
            - name: override
              type: string
              tags:
                '{{ tag_prefix }}_pii': name
        - $ref: $defs/tables/liam_perritt|lff_sqlserver_bronze|dummy_table_cdc_st
      volumes:
        - $defs/volumes/liam_perritt|lff_sqlserver_bronze|test
